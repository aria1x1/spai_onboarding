# PRD: Tech Update 대시보드

## 1. 목표 (Goal)
GitHub Blog, Hugging Face Blog, LangChain Blog의 업데이트 소식과 하루 단위 테크 뉴스를 한 곳에서 요약해 보여주는 대시보드를 제공한다.
사용자가 여러 사이트를 매일 직접 돌아다니며 확인하는 대신, 하나의 화면에서 짧은 요약으로 최신 동향을 빠르게 파악할 수 있게 하는 것이 목표다.

성공 지표(예):
- 사용자가 하루 1회 방문으로 3개 소스 + 데일리 뉴스의 핵심을 파악할 수 있다.
- 원문 대비 요약을 읽는 데 걸리는 시간이 원문 전체를 읽는 시간보다 현저히 짧다(체감 기준 1/5 이하).

## 2. 사용자 (Users)
- **주 사용자**: AI/ML, 개발 트렌드를 꾸준히 팔로업해야 하는 개발자, 데이터 사이언티스트, PM. 핀테크 탭 이용자는 위 사용자군 중 핀테크 도메인에도 관심이 있는 인원.
- **사용 시나리오**: 출근 후 또는 하루 중 짬이 날 때 대시보드를 열어 지난 24시간~며칠 내 업데이트를 훑어보고, 관심 있는 항목만 원문 링크로 이동해 자세히 읽는다. 로그인한 사용자는 관심 있는 항목을 북마크해두거나, 낯선 핀테크 용어를 AI 챗봇에게 바로 물어본다.
- **니즈**: 여러 블로그/뉴스를 개별 방문할 시간이 없음, 핵심만 빠르게 캐치하고 싶음, 나중에 다시 볼 항목을 표시하고 싶음, 낯선 전문 용어를 그 자리에서 질문하고 싶음.
- **로그인 사용자 vs 비로그인 사용자**: 뉴스 피드 열람은 로그인 없이 누구나 가능하다. 북마크와 AI 챗봇(및 아카이브)은 이메일 매직링크로 로그인한 사용자만 사용할 수 있다.

## 3. 핵심 기능 (Top 3)

### 3.1 소스별 업데이트 수집 및 요약
- GitHub Blog, Hugging Face Blog, LangChain Blog의 신규 포스트를 주기적으로 수집(RSS/크롤링)한다.
- 각 포스트를 3~5줄 내외로 자동 요약하여 카드 형태로 노출한다.
- 원문 링크, 발행일, 출처 로고/뱃지를 함께 표시한다.

### 3.2 데일리 테크 뉴스 요약
- 하루 단위로 주요 테크 뉴스를 수집해 다이제스트 형태로 요약 제공한다.
- 날짜별로 묶어서 "오늘의 뉴스" / "지난 뉴스" 형태로 구분해 보여준다.

### 3.3 통합 피드 뷰 & 필터링
- 3개 블로그 소스 + 데일리 뉴스를 하나의 타임라인(피드)으로 통합해서 볼 수 있다.
- 소스별 필터(GitHub / Hugging Face / LangChain / 테크 뉴스)와 날짜 필터를 제공한다.

### 3.4 Fintech 탭 — 핀테크 데일리 뉴스 수집
- 별도의 **Fintech** 탭을 신설하여 아래 4개 사이트의 데일리 뉴스를 주기적으로 수집한다.
  - fintech.global
  - fintechfutures.com
  - thisweekinfintech.com
  - fintechtimes.co.kr
- 각 사이트를 별도 소스로 구분(배지)하되, Fintech 탭에서는 4개 소스를 하나의 통합 피드로 보여주고 소스별/날짜 필터를 제공한다. (3.1/3.3과 동일한 카드형 UI, 요약 방식을 재사용한다.)
- 기존 3개 블로그 소스(GitHub/Hugging Face/LangChain) 및 데일리 테크 뉴스와는 분리된 별도 탭으로, 메인 통합 피드(홈)에는 노출되지 않는다.

### 3.5 북마크 (로그인 필요)
- 로그인한 사용자는 홈/소스별/Fintech 탭 어디서든 카드에 있는 북마크 버튼으로 항목을 저장할 수 있다.
- 북마크는 Supabase에 사용자별로 저장되어, 같은 계정으로 다른 기기에서 로그인해도 동일하게 보인다.
- "북마크" 탭에서 내가 저장한 항목만 모아서 최신순으로 볼 수 있고, 각 항목에서 북마크를 해제할 수 있다.
- 비로그인 사용자가 북마크 버튼을 누르면 로그인(매직링크) 화면으로 안내한다.

### 3.6 핀테크 용어 AI 챗봇 + 아카이브 (로그인 필요)
- 로그인한 사용자가 핀테크 관련 용어/개념을 질문하면, 핀테크 분야에 특화된 프롬프트로 답변하는 AI 챗봇을 제공한다.
- 질문 1건 = 답변 1건 단위의 Q&A로 동작한다 (여러 턴에 걸친 대화 맥락 유지는 v1 범위 밖).
- 질문/답변 쌍은 사용자별로 자동 저장되며, "AI 챗봇" 탭 내 "아카이브" 영역에서 과거에 물어본 질문과 답변을 다시 확인할 수 있다.
- 비로그인 사용자는 챗봇 화면 진입 시 로그인(매직링크) 화면으로 안내한다.

## 4. 화면 구성 (Screens)

1. **메인 대시보드 (홈)**
   - 상단: 필터 바 (소스 선택, 기간 선택, 검색)
   - 중앙: 카드형 통합 피드 (최신순 정렬), 각 카드에 출처/제목/요약/날짜/원문 링크
   - 사이드 또는 상단 요약 영역: "오늘의 데일리 뉴스 다이제스트" 하이라이트

2. **소스별 상세 뷰**
   - 특정 소스(GitHub / Hugging Face / LangChain)만 선택했을 때 해당 소스의 포스트 목록을 리스트/카드로 표시

3. **데일리 뉴스 아카이브**
   - 날짜별로 묶인 뉴스 다이제스트 리스트, 날짜 선택 시 해당 일자 요약 상세 표시

4. **(선택) 항목 상세 모달/페이지**
   - 카드를 클릭하면 요약 전문 + 원문 링크 + (있다면) 관련 태그 표시

5. **Fintech 피드**
   - 4개 핀테크 소스가 통합된 피드. 상단 필터 바(소스 선택, 기간 선택)는 홈과 동일한 컴포넌트를 재사용

6. **로그인 (매직링크)**
   - 이메일 입력 → 매직링크 발송 안내 → 링크 클릭 시 로그인 완료. 비밀번호 없음.
   - 북마크 버튼 또는 AI 챗봇 탭 진입 시 비로그인 상태라면 이 화면으로 유도

7. **북마크 목록**
   - 로그인 사용자가 저장한 항목만 최신순으로 나열, 항목별 북마크 해제 가능

8. **AI 챗봇 (핀테크 용어)**
   - 질문 입력창 + 답변 표시 영역, 하단/사이드에 "아카이브"로 과거 질문·답변 목록 표시, 클릭 시 해당 답변 다시 표시

## 5. 제외 범위 (Out of Scope)
- 개인화 추천 기능 (v1에서는 북마크 외 개인 맞춤 로직 없음)
- 소셜 로그인(OAuth), 비밀번호 로그인 (이메일 매직링크만 지원)
- 좋아요/댓글/공유 등 북마크 외 사용자 상호작용 기능
- 알림(이메일, 푸시, Slack 연동 등) 기능
- GitHub Blog, Hugging Face Blog, LangChain Blog, 데일리 테크 뉴스, 4개 핀테크 소스 외 추가 소스 확장 (v2 이후 검토)
- AI 챗봇의 멀티턴 대화(이전 질문을 참조하는 후속 질문) 및 스트리밍 응답 UI (v1은 질문-답변 단건 처리)
- 다국어 지원 (초기 버전은 한국어 또는 영어 단일 언어로 한정)
- 모바일 네이티브 앱 (반응형 웹으로 대응, 별도 앱 없음)
- 요약 품질에 대한 사람 검수/편집 워크플로우 (자동 요약 결과를 그대로 노출)
- 자체 서버 인프라 구축/운영 (Supabase, Vercel 등 매니지드 서비스를 활용하며 별도 서버를 직접 운영하지 않음)

## 6. 검증 기준 (Acceptance Criteria)
- [ ] GitHub Blog, Hugging Face Blog, LangChain Blog 3개 소스의 신규 포스트가 정해진 주기(예: 1일 1회 이상) 내에 대시보드에 반영된다.
- [ ] 각 포스트에 자동 생성된 요약(3~5줄)이 노출되고, 원문 링크 클릭 시 실제 원문으로 정상 이동한다.
- [ ] 데일리 테크 뉴스가 날짜별로 구분되어 최소 1일 1회 업데이트된다.
- [ ] 통합 피드에서 소스별 필터링 시 선택한 소스의 항목만 정확히 필터링되어 보인다.
- [ ] 날짜 필터 적용 시 지정한 기간 내 항목만 표시된다.
- [ ] 신규 소스 추가 없이도 3개 블로그 + 데일리 뉴스가 안정적으로 하루 이상 중단 없이 수집/표시된다(수집 실패 시 에러 없이 이전 데이터 유지).
- [ ] 요약문이 원문 핵심 내용을 왜곡 없이 담고 있는지 샘플 검토(예: 10개 포스트 중 8개 이상 적절하다고 평가) 기준을 통과한다.
- [ ] Fintech 탭에서 4개 핀테크 소스(fintech.global / FinTech Futures / This Week in Fintech / 핀테크타임스)의 신규 게시물이 1일 1회 이상 반영되고, 소스 중 하나가 수집에 실패해도 나머지 소스와 기존 데이터는 그대로 유지된다.
- [ ] 비로그인 상태에서 북마크 버튼 또는 AI 챗봇 탭 진입 시 로그인(매직링크) 화면으로 안내된다.
- [ ] 로그인 후 북마크한 항목이 "북마크" 탭에 즉시 반영되고, 다른 기기에서 같은 계정으로 로그인해도 동일한 북마크 목록이 보인다.
- [ ] 북마크 해제 시 목록에서 즉시 제거되고 재방문 시에도 사라진 상태가 유지된다.
- [ ] AI 챗봇에 핀테크 용어를 질문하면 관련성 있는 답변이 반환되고, 해당 질문/답변이 아카이브 목록에 저장되어 재방문 시에도 조회 가능하다.
- [ ] 계정 A로 저장한 북마크/챗봇 아카이브가 계정 B에는 보이지 않는다(사용자별 데이터 분리 확인).

## 7. 기술 아키텍처 (참고)
- **프론트엔드**: 순수 HTML/CSS/JS (`index.html`, `style.css`, `app.js` 3개 파일 고정, 프레임워크·빌드 스텝 없음), Vercel에 정적 호스팅
- **데이터 저장소**: Supabase(Postgres). 여러 기기/방문에서 동일한 수집 데이터를 봐야 하므로 브라우저 localStorage는 대시보드 데이터 저장에 사용하지 않고, 사용자 개인 설정(필터 상태 등)에만 사용
- **수집/요약 실행**: RSS/HTML 수집(GitHub/Hugging Face/LangChain Blog + 4개 핀테크 소스) 및 Claude API 요약은 브라우저가 아닌 Vercel Serverless Function에서 서버사이드로 실행 (CORS 회피, API 키 비노출 목적). Vercel Cron Jobs로 주기 실행
- **인증**: Supabase Auth의 이메일 매직링크만 사용 (비밀번호/OAuth 없음). 뉴스 피드 열람은 비로그인으로도 가능하고, 북마크·AI 챗봇 기능만 로그인을 요구한다.
- **북마크/챗봇 아카이브 저장**: 로그인된 사용자의 Supabase 세션을 이용해 클라이언트에서 직접 읽고 쓰되, RLS로 본인 소유 행만 접근 가능하도록 제한한다 (계정 간 데이터 분리).
- **AI 챗봇 응답 생성**: Claude API 호출은 별도 Vercel Serverless Function에서 서버사이드로 실행 (API 키 비노출 목적). 핀테크 용어에 특화된 시스템 프롬프트를 사용하고, 응답을 받은 후 질문/답변을 사용자 계정에 연결해 저장한다.
- 상세 규칙은 `CLAUDE.md` 참고. 이 구조는 최초 검토했던 로컬 Python/FastAPI 프로토타입을 대체한다 (2026-08-12 결정). 로그인/북마크/챗봇 확장은 2026-08-13 결정.
